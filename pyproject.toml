[project]
name = "fzgx-tools"
version = "0.1.0"
description = "Tooling for the F-Zero GX (GFZE01) matching decompilation"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8", "capstone>=5", "pyyaml>=6"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["tools"]
